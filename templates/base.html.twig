<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block body %}

            <header class="sticky -top-px bg-base font-mont text-base text-truewhite h-20 z-30 px-4">
                <div class="container h-full mx-auto items-center flex justify-between">
                    <a href="{{path('app_home')}}"><img class="max-h-14 w-auto" src="{{asset('build/images/logo.svg')}}" alt="logo du garden-coworking"></a>

                    <button id="burger" class="lg:hidden">
                        <div class="bg-white"></div>
                        <div class="bg-white"></div>
                        <div class="bg-white"></div>
                        <div class="bg-white"></div>
                    </button>

                    <nav id="nav" class="text-white fixed z-20 top-16 pt-4 lg:pt-0 bottom-0 -right-full w-60 lg:w-5/12 lg:static transition-all ease duration-500">
                        <ul class="lg:flex justify-between">
                            <li>
                                <a class="block p-3 transition-all hover:text-primary" href="#">Boutique</a>
                            </li>
                            <div class="lg:flex">
                                <li class="self-center flex justify-center mt-5 lg:mr-2 lg:mt-0">
                                    <a class="block bg-primary rounded-md py-1 px-8 transition-all hover:bg-indigo-700" href="{{path('app_register')}}">Inscription</a>
                                </li>
                                <li class="self-center flex justify-center mt-5 lg:mr-2 lg:mt-0">
                                    <a class="block bg-primary rounded-md py-1 px-8 transition-all hover:bg-indigo-700" href="{{path('app_login')}}">Connexion</a>
                                </li>
                            </div>
                        </ul>
                    </nav>
                </div>
            </header>
        
        {% endblock %}
    </body>
</html>
